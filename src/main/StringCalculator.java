package main;

public class StringCalculator {
    public static int Add(String text_input) {
        if (text_input == null || text_input.length() == 0) {
            return 0;
        }
        return '\0';
    }
}
